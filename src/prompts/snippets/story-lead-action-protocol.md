## Action Protocol
Choose the smallest next bounded action that safely moves the story forward.
Return exactly one JSON object with no markdown fences, no prose, no arrays, and no wrapper keys.

Valid action shapes:
{{RESULT_CONTRACT_SCHEMA}}
