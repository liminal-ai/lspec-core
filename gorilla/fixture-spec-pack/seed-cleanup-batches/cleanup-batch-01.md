# Cleanup Batch 01

- APPROVED: Tighten `target-codebase/README.md` so the verification scripts and sample modules are described in one short paragraph.
- APPROVED: Remove any stale mention of "CSV export" if it appears anywhere under `target-codebase/`.
- REVIEWED: Keep changes inside `target-codebase/` only.
