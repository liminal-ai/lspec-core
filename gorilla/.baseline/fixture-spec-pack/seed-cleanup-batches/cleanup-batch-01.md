# Cleanup Batch 01

1. Tighten README wording so the verification scripts are described in one short paragraph.
2. Remove any stale mention of "CSV export" if it appears in the fixture target codebase.
3. Keep changes inside `target-codebase/` only.
